// Placeholder test until we wire real widget tests for the auth flow.
// `flutter test` should pass on a fresh checkout, so we keep this trivial.

import 'package:flutter_test/flutter_test.dart';

void main() {
  test('placeholder', () {
    expect(1 + 1, equals(2));
  });
}
